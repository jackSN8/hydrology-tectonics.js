let cells = [][];
let resolution = 10;
let rowLength = 40;

function setup()
{
  createCanvas(resolution*rowLength,resolution*rowLength);

  textSize(30);
  textAlign(CENTER);
  frameRate(15);
}

function draw()
{
  background(127);
}